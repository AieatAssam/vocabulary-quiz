export const environment = {
  // OpenAI API key should be provided at runtime (never hardcoded)
  openAIApiKey: '',
  // Optional: override the default OpenAI endpoint (e.g., for OpenRouter)
  openAIEndpoint: '',
  // Optional: override the default model (default: 'gpt-4o-mini')
  openAIModel: 'gpt-4o-mini',
};
